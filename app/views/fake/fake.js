define(['app'], function(app) {
    'use strict';
    return ["$q", "$element", "$compile", "$scope", "$route", "realm", 'component',
        function ($q, $element, $compile, $scope, $route, realm) {

            console.log('helll')
    }];
});