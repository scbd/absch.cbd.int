machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t absch-cbd-int .

test:
  override:
    - docker run -d -p 2010:2010 absch-cbd-int; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:2010